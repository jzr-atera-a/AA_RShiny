module:
  id: "api_config"
  name: "API Configuration"
  description: "Configure BigQuery and Claude API credentials and settings"
  version: "1.0.0"
  author: "Template"
  
  enabled: true
  
  menu:
    label: "API Config"
    icon: "cog"
    tabname: "api_config"
  
  dependencies:
    packages:
      - shiny
      - shinydashboard
      - bigrquery
      - httr
      - jsonlite
  
  api:
    required: []
    optional: ["bigquery", "claude"]
