# modules/Funding Programmes/funding_about/server.R

funding_about_server <- function(id, api_manager) {
  moduleServer(id, function(input, output, session) {
    # Static informational tab - no server logic needed
  })
}
