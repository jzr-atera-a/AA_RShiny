# modules/Exercise Tracker/exercise_about/server.R

exercise_about_server <- function(id, api_manager) {
  moduleServer(id, function(input, output, session) {
    # Static informational tab - no server logic needed
  })
}
