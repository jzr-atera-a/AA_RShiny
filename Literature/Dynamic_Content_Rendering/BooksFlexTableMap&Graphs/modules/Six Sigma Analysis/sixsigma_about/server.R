# modules/Six Sigma Analysis/sixsigma_about/server.R

sixsigma_about_server <- function(id, api_manager) {
  moduleServer(id, function(input, output, session) {
    # Static documentation module - no server logic needed.
  })
}
