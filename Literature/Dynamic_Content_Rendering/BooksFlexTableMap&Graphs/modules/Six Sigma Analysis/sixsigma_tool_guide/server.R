# modules/Six Sigma Analysis/sixsigma_tool_guide/server.R

sixsigma_tool_guide_server <- function(id, api_manager) {
  moduleServer(id, function(input, output, session) {
    # Static reference guide - no server logic needed.
  })
}
