# modules/Strategic Analysis/diagram_tool_guide/server.R

diagram_tool_guide_server <- function(id, api_manager) {
  moduleServer(id, function(input, output, session) {
    # Static reference guide - no server logic needed.
  })
}
